const tripStatus = ['PENDING', 'APPROVED', 'REJECTED'];

const locationFields = ['depart_location_id'];

const accommodationFields = ['accommodation_id'];

module.exports = { tripStatus, locationFields, accommodationFields };
